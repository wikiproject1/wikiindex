{
  "name": "wikitrust-index",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "WikiTrust Index - A comprehensive web application that analyzes Wikipedia articles for content quality, reliability, and trustworthiness",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "npm run build && npm run preview -- --host 0.0.0.0 --port 8000",
    "serve": "npm run preview -- --host 0.0.0.0 --port 8000"
  },
  "dependencies": {
    "axios": "^1.11.0",
    "bootstrap": "^5.3.7",
    "bootstrap-icons": "^1.11.3",
    "vue": "^3.5.18"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.1",
    "sass": "^1.90.0",
    "vite": "^7.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
